package com.monmi.production.repository;

import com.monmi.production.domain.ProductComb;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ProductCombRepository extends JpaRepository<ProductComb, Integer> {
}
